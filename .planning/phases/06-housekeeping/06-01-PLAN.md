# Phase 6 Plan 06-01 — Audit + retention

**Goal:** Record who uploaded/downloaded/deleted; sweep home by keep_last **or** max age.

**Requirements:** AUD-01, RET-01

## Tracer

Upload then download → GET `/v1/audit` has those rows and no password/api_key. Three local archives + `KeepLast=2` → `SweepOnce` leaves two. Fake bucket object older than max age is deleted from the prefix.

## Tasks

1. sqlite `audit` + Insert/List; wire upload/download/delete; GET `/v1/audit`; HTML recent table
2. `DELETE /v1/archives/{id...}`; `blob.Store.Delete`; VPS file+row vs bucket DeleteObject
3. `retain.Pick` (either rule); `SweepOnce` + ticker; staging grace sweep as incident log
4. Config `GFS_KEEP_LAST` / `GFS_MAX_AGE_DAYS` defaults 20 / 90
5. Tests: audit no secrets, keep_last, max_age, vps-s3 prefix delete; no live bucket

## Out of scope

UI polish, per-user retention, trusted proxies
