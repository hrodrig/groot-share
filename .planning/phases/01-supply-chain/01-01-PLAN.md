# Phase 1 Plan 01-01 — Copy packaging

**Goal:** Repo has groot-trigger Make/Docker/CI/GoReleaser/golangci/gitignore/contrib, renamed to **gfs**.

**Requirements:** SUP-01, SUP-02, SUP-03, SUP-04 (partial — AGENTS.md already exists)

## Tracer

`test -f GNUmakefile && grep -q APP_NAME.*gfs GNUmakefile`

## Tasks

1. Copy and rename from `/Volumes/Data/addlink/github/groot-trigger`:
   - `Makefile`, `GNUmakefile` (`APP_NAME=gfs`, `MODULE=github.com/hrodrig/groot-share`, `COVER_MIN=60`, help string)
   - `Dockerfile`, `Dockerfile.release`, `.dockerignore`
   - `.goreleaser.yaml` (binary `gfs`, image `ghcr.io/hrodrig/gfs`)
   - `.golangci.yml` (unchanged dialect)
   - `.github/workflows/ci.yml`, `release.yml`
   - `.gitignore` (whitelist; keep `.planning/` and `.claude/`)
   - `LICENSE` (MIT, hrodrig 2026)
   - `contrib/` man + BSD stubs renamed `gfs`
2. `VERSION` = `0.1.0`
3. Short `CHANGELOG.md` / `CONTRIBUTING.md` / `SECURITY.md` in trigger shape
4. Do **not** copy `deploy/k8s` or trigger `internal/`

## Verify

- Files exist and contain `gfs` not `groot-trigger` (except "mirrors groot-trigger" comments)
- `make goreleaser-check` skips or passes without origin

## Out of scope

`cmd/gfs` (plan 01-02), HTTP, SQLite
