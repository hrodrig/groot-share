# Phase 1 Plan 01-02 — Stub binary

**Goal:** `make ci` is green on a version-only `cmd/gfs`.

**Requirements:** SUP-01 (ci), SPEC-01 (AGENTS already points at SPEC)

## Tracer

`make test` and `./bin/gfs version` print `gfs …`

## Tasks

1. `go.mod` module `github.com/hrodrig/groot-share`, Go 1.26.5, no k8s deps
2. `cmd/gfs/main.go` — ldflags version/commit/branch/buildDate; `version|--version|-V`; bare args: message that HTTP is Phase 2, exit 0
3. `cmd/gfs/main_test.go` — TestRunVersion
4. `go test ./...` ; `make fmt-check` ; `make ci` if golangci available

## Verify

- `make build && ./bin/gfs version` exits 0
- `make test` passes
- Coverage ≥ COVER_MIN 60 on this stub

## Out of scope

Listen, healthz, config env (Phase 2)
