# Plan 08-02: SFTP source — UI pill + docs

**Phase:** 8 — SFTP inbox watcher
**Depends on:** 08-01
**Requirement:** ING-04

## Goal

Captures clearly labels SFTP ingests; operators know how to wire the inbox.

## Tasks

1. **UI** — `.pill-sftp` in `html.go` (distinct from HTTP blue — e.g. teal/slate per design tokens); template already renders `{{.Source}}`
2. **Sort** — confirm `source` sort orders `http`, `sftp`, `s3` lexically (no change unless tests fail)
3. **JSON API** — list/upload responses expose `"source":"sftp"` (covered by 08-01; smoke test here)
4. **Docs** — `docs/SPECIFICATIONS.md` §4 + §5 config table (`GFS_SFTP_INBOX`, `GFS_SFTP_POLL`); README one paragraph + link to groot-selfhosted sftp-vps
5. **CHANGELOG** — v0.3.0 entry

## Verification

- HTML test or identity test: page contains `pill-sftp` when fixture has `source=sftp`
- `go test ./...`
