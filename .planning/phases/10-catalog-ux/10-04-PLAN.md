# Plan 10-04 — Responsive table → card layout

**Phase:** 10 — Operational catalog UX
**Goal:** On narrow viewports, render the Captures archive list as **cards**
instead of a horizontally-scrolling 6-column table, with **Download** as a
primary action on each card and copy-download-link preserved.

**Carried from 10-01..10-03:** the `homeTmpl` archive `<table class="grid">`
with sortable columns and per-row icon actions; the `.grid` / `.pill` /
`.actions` CSS; the `data-confirm` delete dialog.

**Locked in 10-04-CONTEXT.md (D-03, D-04, D-14).**

---

## Tasks

### T1 — Card markup in `homeTmpl`

**Files**
- `internal/server/identity.go` — after the `</table>` / `</div>` and before
  the pager `{{if gt .Pager.Total 0}}`, add a cards list.

Add a `<ul class="archive-cards">` rendering the **same** `.Items` range:

```html
<ul class="archive-cards">
{{range .Items}}
<li class="archive-card">
  <div class="card-title mono" title="{{.Key}}">{{.Key}}</div>
  <div class="card-meta">
    <span class="pill pill-{{.Source}}">{{.Source}}</span>
    {{if .Storage}}<span class="pill pill-{{.Storage}}">{{.Storage}}</span>{{end}}
    <span class="muted tabular">{{humansize .Size}}</span>
    <span class="muted tabular">{{.CreatedAt.UTC.Format "2006-01-02 15:04"}}</span>
  </div>
  <div class="card-actions">
    <a class="btn" href="/v1/archives/{{.ID}}/file">Download</a>
    <button class="btn btn-quiet copy-link" type="button"
            data-copy-url="{{$.BaseURL}}/v1/archives/{{.ID}}/file"
            title="Copy download link"
            aria-label="Copy download link for {{.Key}}">Copy link</button>
    {{if $.CanDelete}}
    <form method="post" action="/v1/archives/{{.ID}}/delete"
          data-confirm="Delete {{.Key}}? This cannot be undone.">
      <button class="btn btn-danger-quiet" type="submit"
              aria-label="Delete {{.Key}}">Delete</button>
    </form>
    {{end}}
  </div>
</li>
{{end}}
</ul>
```

Notes:
- `Download` is a full-width primary `.btn` (satisfies D-04).
- `data-copy-url` reuses the existing `.copy-link` JS handler (already wired
  in `homeTmpl`'s script — no new JS needed).
- `data-confirm` reuses the existing modal confirm dialog.
- The cards `<ul>` renders **inside** the same `{{if .Items}}` block as the
  table, and only when there are items.

**Verification:** go build + tests; template still parses.

### T2 — CSS: table-off / cards-on below 720px

**Files**
- `internal/server/html.go` — append to/near the data-grid section (~line 607).

Add:

```css
/* ---- archive cards (narrow viewports) ---- */
.archive-cards { display: none; }

@media (max-width: 719px) {
  .table-wrap { display: none; }
  .archive-cards {
    display: flex;
    flex-direction: column;
    gap: 12px;
    list-style: none;
    margin: 0;
    padding: 16px;
  }
  .archive-card {
    display: flex;
    flex-direction: column;
    gap: 10px;
    padding: 14px 16px;
    border: 1px solid var(--line);
    border-radius: var(--radius);
    background: var(--surface);
  }
}
```

Add `.card-title`, `.card-meta`, `.card-actions` base rules (outside the
media query so they are reusable):

```css
.archive-card .card-title { font: 13px/1.4 var(--mono); word-break: break-all; }
.archive-card .card-meta { display: flex; flex-wrap: wrap; align-items: center; gap: 6px; font-size: 13px; }
.archive-card .card-actions { display: flex; gap: 8px; margin-top: 2px; }
.archive-card .card-actions form { margin: 0; }
.archive-card .card-actions .btn { flex: 0 0 auto; }
.archive-card .card-actions a.btn { flex: 1 1 auto; text-align: center; }
```

The `.btn` class already exists and gives the primary button the accent style;
`.btn-quiet` / `.btn-danger-quiet` exist too.

**Verification:** no horizontal scroll at ≤ 719px; cards show name, pills,
size, timestamp, and a Download button; table hidden below the breakpoint.

### T3 — CSS media query already present check

Confirm the existing `@media (max-width: 720px)` blocks live in `html.go`
(summary, upload-cta, filter). 10-04 adds a new `@media (max-width: 719px)`
block specifically for the grid/cards switch — keep it adjacent to the
data-grid section for discoverability.

### T4 — Tests

**Files**
- `internal/server/dashboard_test.go` — extend.

**Assertions**
- Uploader/admin renders `class="archive-cards"` and at least one
  `class="archive-card"` when there are items, with a `Download` button
  (text `>Download<` or `href="/v1/archives/{id}/file"`).
- Viewer still renders the table (wide CSS path) — assert the table is
  present regardless of role (it is role-gated only by `CanDelete` for the
  delete action, not by viewer).
- If `CanDelete` false, the card has no delete form.

Follow the existing `dashboard_test.go` helper style (`identServer`,
`loginCookie`, `createArchive`).

**Verification:** gofmt, vet, test green.

### T5 — `make ci` green

- `gofmt -l` empty
- `go vet ./...` empty
- `go test ./... -race -count=1` green
- `golangci-lint run ./...` clean
- `gocyclo` ≤ 14 (cards are template markup, no new Go functions)
- Coverage floor ≥ 10-03 baseline

### T6 — Docs sync

**Files**
- `docs/SPECIFICATIONS.md` §4 — extend the Captures page paragraph to note
  the responsive card layout (table on desktop, cards ≤ 719px, Download
  primary per card).
- `CHANGELOG.md` — Phase 10 / UX-04 bullet under `[Unreleased]`.
- `README.md` — feature bullet (responsive cards).

**Do NOT** bump `VERSION`.

---

## Commit order (short, per functional thing)

1. `docs(plan): 10-04 responsive cards — CONTEXT + PLAN`
2. `feat(ui): archive cards markup for narrow viewports` (T1)
3. `style(ui): responsive card layout — table off / cards on ≤ 719px` (T2)
4. `test(ui): archive cards visible to uploader, delete gated by CanDelete`
   (T4)
5. `docs: 10-04 responsive cards in SPEC §4 + CHANGELOG + README` (T6)
6. `docs(plan): 10-04 SUMMARY + STATE/ROADMAP sync`

Each commit compiles; tests pass for touched packages.

---

## Out of scope for 10-04 (deferred)

- Share-link admin UI on cards → **10-07** (D-14 slot reserved)
- Activity filters/export, settings safety, destructive typed-confirm → **10-05**
- Manifest peek → **10-06**

---

## Verification (binary)

- [ ] `make ci` exits 0
- [ ] `gofmt -l` empty; `go vet ./...` empty; `golangci-lint` clean
- [ ] `go test ./... -race` green; coverage ≥ 10-03 baseline
- [ ] `git status` clean after the 6 commits
- [ ] Manual: at ≤ 719px the archive list is cards (no horizontal scroll),
      each card has Download primary + Copy link, delete only when CanDelete.
