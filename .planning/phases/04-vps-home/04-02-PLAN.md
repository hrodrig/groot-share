# Phase 4 Plan 04-02 — Vanilla HTML list/upload

**Goal:** GET `/` lists archives and can upload/download. Shared CSS with login. Usable, not a design system.

**Requirements:** LIST-01, LIST-03

## Tracer

Logged-in GET `/` shows the uploaded filename and a download link.

## Tasks

1. Shared layout CSS (login + home)
2. Home: table + multipart form `file` → POST `/v1/archives` then redirect `/`
3. Download link `/v1/archives/{id}/file`

## Out of scope

SPA, shadcn, gsd-ui-phase polish
