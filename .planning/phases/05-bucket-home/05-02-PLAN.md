# Phase 5 Plan 05-02 — List from prefix, foreign keys

**Goal:** `vps-s3` listing is ListObjectsV2 under `GFS_S3_PREFIX`. Cluster `upload.s3` keys appear (`source=s3`). HTTP dated keys `source=http`. Staging is omitted. Download by full key (`{id...}`).

**Requirements:** ING-02, STOR-02 (list half)

## Tracer

Seed memory blob with `captures/cluster-run.tar.gz` (never ingested over HTTP) → GET `/v1/archives` includes it with `source=s3`. HTTP ingest key matches dated scheme → `source=http`. GET `/v1/archives/{key}/file` returns bytes.

## Tasks

1. List via Blobs.List(prefix); map to Archive; sort newest first
2. `source` from key shape (dated 32-hex → http, else s3)
3. Mux `GET /v1/archives/{id...}`; download GetObject, else transit staging file
4. HTML download href uses listed `id` (full key)
5. Tests: foreign key in list, dated key source=http, download slashed id; vps tests still pass

## Out of scope

Retention delete, audit, changing groot objectKey
